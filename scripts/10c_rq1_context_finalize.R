# Compatibility notice: context-conditioned modelling is now finalized inside
# the canonical RQ2 analysis.  This former RQ1 finalizer is intentionally inert
# so old generated artifacts cannot be reused.
message("RQ1 context finalizer retired; run scripts/12_rq2_analysis.R instead.")
